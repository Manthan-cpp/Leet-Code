#include <string>
class Solution {
public:
    int strStr(string haystack, string needle) {
        bool found=false;int index=-1;
        index = haystack.find(needle);
        return index;
    }
};